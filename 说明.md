# ImageGen 使用与打包说明

> 开发、打包、数据目录、更新记录 — 看这一份就够了。

当前版本：**1.0.1**（见 `pubspec.yaml`）

---

## 环境要求

- [Flutter SDK](https://docs.flutter.dev/get-started/install) 3.22+（推荐 3.35+）
- Windows 10/11 + Visual Studio「使用 C++ 的桌面开发」
- 首次克隆项目后：

```powershell
cd d:\exp\shengtu\imagegen_flutter
.\tool\init.ps1          # 或 flutter pub get
```

---

## 日常开发

```powershell
flutter run -d windows
```

---

## 打包方式（三选一）

### 1. 标准 Release（文件夹，开发调试用）

```powershell
flutter build windows --release
```

| 产物 | 路径 |
|------|------|
| 主程序 | `build\windows\x64\runner\Release\imagegen.exe` |
| 依赖 | 同目录下的 dll、`data\` 文件夹 |

**整份 `Release` 文件夹必须一起拷贝**，单独一个 exe 跑不起来。

---

### 2. 绿色版（文件夹 / zip，推荐分发）

```powershell
.\tool\build_portable.ps1
```

| 产物 | 路径 |
|------|------|
| 绿色版文件夹 | `build\ImageGen\` |
| 压缩包 | `build\ImageGen-{版本}-portable.zip` |

解压或拷贝整个文件夹，运行 `imagegen.exe` 即可，**无需安装**。

---

### 3. 单文件 exe（只有一个 exe，推荐便携）

```powershell
.\tool\build_single.ps1
```

| 产物 | 路径 |
|------|------|
| 单文件绿色版 | `build\ImageGen-{版本}.exe` |

- 只有一个 exe，双击运行
- **首次启动**会解压到 `%LOCALAPPDATA%\ImageGen\portable\{hash}\`（约 50MB）
- **之后启动**直接用缓存，会快很多
- 用户数据（配置、历史、图片）仍在应用数据目录，与绿色版共用

> 需要本机已安装 Visual Studio C++ 编译器（用来编译启动器 stub）。
> 打包时会自动从 `icon.ico` 同步图标到 exe（含单文件外层启动器）。

---

### 图标

项目根目录的 `icon.ico` 是主图标源。打包脚本会自动执行 `dart run flutter_launcher_icons` 同步到：

- `windows/runner/resources/app_icon.ico` → `imagegen.exe` 图标
- 单文件版启动器 → `ImageGen-{版本}.exe` 图标

若图标显示不对，改 `icon.ico` 后重新打包即可。

### 4. 安装包 Setup（可选，需 Inno Setup）

```powershell
.\tool\build_installer.ps1
```

| 产物 | 路径 |
|------|------|
| 安装程序 | `build\installer\ImageGen Setup {版本}.exe` |

需先安装 [Inno Setup 6](https://jrsoftware.org/isinfo.php)。  
**不要用 IExpress 或 staging 目录复制**，只从 `Release` 目录打包。

---

## 打包产物对照

| 方式 | 命令 | 输出 | 适用场景 |
|------|------|------|----------|
| Release | `flutter build windows --release` | `Release\` 文件夹 | 本地测试 |
| 绿色版 | `.\tool\build_portable.ps1` | 文件夹 + zip | 解压即用 |
| 单文件 | `.\tool\build_single.ps1` | 单个 exe | 只发一个文件 |
| 安装包 | `.\tool\build_installer.ps1` | Setup.exe | 带安装向导 |

---

## 数据与配置目录

| 项目 | 路径 |
|------|------|
| 主配置（API、主题、设置） | `%APPDATA%\com.origami.imagegen\config.json` |
| 历史记录（按周拆分） | `%APPDATA%\com.origami.imagegen\history\2026-W36.json` … |
| 运行日志 | `%APPDATA%\com.origami.imagegen\app.log` |
| 生成图片 | 默认 `%APPDATA%\com.origami.imagegen\images\`（可在设置里改） |
| 网格缩略图缓存 | `{图片目录}\.thumbs\` |
| 单文件版解压缓存 | `%LOCALAPPDATA%\ImageGen\portable\{hash}\` |
| Electron 旧版（可自动迁移） | `%APPDATA%\imagegen-origami\` |

### 历史记录说明

- `config.json` **不再存历史**，只存设置
- 历史按 **ISO 周** 写入 `history/2026-W36.json` 这类文件
- 应用内列表上限可在 **系统设置 → 历史列表上限** 调整（50 / 100 / 200 / 500 / 1000，默认 200）
- 超出上限的条目仍保留在周文件里，**不删磁盘图片**

---

## 常见问题

**Q：换了项目路径后 Windows 编译报错 CMake cache？**

```powershell
Remove-Item -Recurse -Force build\windows
flutter build windows --release
```

**Q：单文件 exe 第一次打开慢？**  
正常，首次要解压；之后会快。

**Q：绿色版和单文件版用户数据互通吗？**  
互通，都用 `%APPDATA%\com.origami.imagegen\` 下的配置和历史。

---

## 更新日志

### 1.0.1（2026-09-02）

**打包与图标**

- 修复单文件 exe 无图标：启动器嵌入 `icon.ico`
- 打包脚本自动同步 Windows 图标（`flutter_launcher_icons`）

**性能与存储**

- 网格缩略图：生成约 512px 缩略图缓存到 `images\.thumbs\`，避免每次打开读完整大图
- 历史配置拆分：`config.json` 只存设置；历史按周写入 `history/YYYY-Www.json`
- 历史列表上限可在系统设置中调整（50 / 100 / 200 / 500 / 1000）
- 启动时最多加载最近 52 周的历史文件

**打包**

- 新增 `tool\build_portable.ps1` — 绿色版文件夹 + zip
- 新增 `tool\build_single.ps1` — 单文件 exe（内嵌压缩包 + 启动器）
- 新增 `tool\build_installer.ps1` + `tool\installer.iss` — Inno Setup 安装包

### 1.0.0

- 从 Electron 版迁移至 Flutter
- 多 API 配置、流式生成、历史网格、灯箱预览
- 参考图上传 / 拖放 / 粘贴
- 5 套主题、并发任务队列、无边框窗口
- 自动迁移 Electron 版 `%APPDATA%\imagegen-origami\` 配置

---

## 项目结构（简要）

```
lib/
  main.dart                 # 入口
  services/
    config_service.dart     # 配置读写
    history_store.dart      # 按周历史存储
    thumbnail_service.dart  # 缩略图缓存
    api_service.dart        # API 请求
    image_service.dart      # 图片保存
  screens/home_screen.dart
  widgets/                  # UI 组件
tool/
  build_portable.ps1        # 绿色版打包
  build_single.ps1          # 单文件 exe 打包
  build_installer.ps1       # Inno Setup 安装包
  launcher/main.cpp         # 单文件 exe 启动器
```
